<?xml version="1.0" encoding="UTF-8"?>
<test-data
        xmlns="http://pmd.sourceforge.net/rule-tests"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">

    <code-fragment id="full-example"><![CDATA[
public class Foo extends Bar {
    int y, z, t;
    String q;

    {
        bar();
        int x;
        switch on
        x {
        when 1 { foo(); break; }
        when 2 { }
        when 3 { bar();
            break; }
        when else { break; }
        }
    }

    public Foo() {
        this(2);
    }

    public Foo(int z) {

        Random r = new Random();
        for (int j = -12; i < z; i++) {
            for (int k = -20; k < 20; k += j) {
                if (r.nextInt() == 42) {
                    System
                            .out
                            .println('Bye');
                    break;
                } else if (k > 12) {
                    continue;
                }
            }
        }

        super.f(z);
    }

    public void foo() {
        int x = 0;
        x++;

        do {
            if (x == 0) {
                x++;
            } else if (x == 1) {
                x = 0;
            } else {
                System.out.println('Bonjour');
                throw new ScaryException('Surprise!');
            }
        } while (x != 2);
    }

    public static void main(String[] args) {
        String k, l, m;
        int x;

        bar();

        do {
            x++;
        } while (x < 2);

        while (x < 4) {
            x++;
        }

        for (int i = 1; x < 6; ) {
            x += i;
        }

        {
            x++;
        }

        int p =
                2 + 4 + 5;

        try {
            x++;
        } catch (ArrayIndexOutOfBoundsException e) {
            e.printStackTrace();
        } catch (ThemAll pokemon) {
            pokemon.train();
        } finally {
            // Do nothing
        }

        bar();
        x = (int) bar();
        /*
         * This is
         * a comment
         */
        x.foobar();
    }
}
    ]]></code-fragment>

    <test-code>
        <description>Full example</description>
        <rule-property name="classReportLevel">40</rule-property>
        <rule-property name="methodReportLevel">5</rule-property>
        <expected-problems>4</expected-problems>
        <expected-messages>
            <message>The class 'Foo' has a NCSS line count of 59 (Highest = 21).</message>
            <message>The constructor '&lt;init&gt;(int)' has a NCSS line count of 10.</message>
            <message>The method 'foo()' has a NCSS line count of 11.</message>
            <message>The method 'main(String[])' has a NCSS line count of 21.</message>
        </expected-messages>
        <code-ref id="full-example"/>
    </test-code>

    <test-code>
        <description>Short ctor and method</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    Foo() {
        // Comment not counted
        foo();
    }
    void foo() {
        // Comment not counted
        bar();
    }
}
        ]]></code>
    </test-code>
</test-data>
