<?xml version="1.0" encoding="UTF-8"?>
<test-data
        xmlns="http://pmd.sourceforge.net/rule-tests"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_1_0.xsd">

    <test-code>
        <description>JUnit Jupiter</description>
        <expected-problems>3</expected-problems>
        <expected-linenumbers>6,7,8</expected-linenumbers>
        <code>
            <![CDATA[
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertEquals;
public class SimpleTest {
    @Test
    public void testX() {
        assertEquals("foo", "bar", "reason"); // this could be just fail("reason")
        assertEquals("foo", "foo", "other reason"); // this should be removed
        assertEquals(next("foo"), "bar"); // wrong argument order
        assertEquals("bar", next("foo")); // good
    }
}
]]>
        </code>
    </test-code>
    <test-code>
        <description>JUnit 4</description>
        <expected-problems>3</expected-problems>
        <expected-linenumbers>6,7,8</expected-linenumbers>
        <code>
            <![CDATA[
import org.junit.Test;
import static org.junit.Assert.assertEquals;
public class SimpleTest {
    @Test
    public void testX() {
        assertEquals("foo", "bar", "reason"); // this could be just fail("reason")
        assertEquals("foo", "foo", "other reason"); // this should be removed
        assertEquals(next("foo"), "bar"); // wrong argument order
        assertEquals("bar", next("foo")); // good
    }
}
]]>
        </code>
    </test-code>
</test-data>