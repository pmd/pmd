<?xml version="1.0" encoding="UTF-8"?>
<test-data
    xmlns="http://pmd.sourceforge.net/rule-tests"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">

    <test-code>
        <description>failure case</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (x != null && x instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>ok</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (x instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>transpose x and null, still bad</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (null != x && x instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>conditional or and !(instanceof)</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (x == null || !(x instanceof String)) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>indexing into array is ok</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (x != null && x[0] instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>test for null on unrelated object is ok</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x) {
        if (x instanceof String && ((String)x).substring(2) != null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>Bug [1843273] False -</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Bug1843273 {
    public boolean isTrue(Boolean value) {
       boolean result = true;

       if (value.booleanValue()) {
           return result;
       } else {
           return ! result;
       }
   }
}
        ]]></code>
    </test-code>

     <test-code>
        <description>Bug 2317099 : False + in SimplifyConditional</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Env {
    public void method(Env env) {
        if (env != null && env.getContext() != null &&
            env.getContext().getContextObject() instanceof String) {
        }
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#1165 SimplifyConditional false positive</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    public void bar(Object[] arguments) {
        if (
        arguments == null ||
        arguments.length < 3 || arguments.length > 4 ||
        !(arguments[0] instanceof String ) ||
        !(arguments[1] instanceof Boolean ) ||
        !(arguments[2] instanceof String ) ||
        arguments.length == 4 && !(arguments[3] instanceof String )
        ) {
            throw new IllegalArgumentException( "" );
        }
    }
}
        ]]></code>
    </test-code>
    
    <test-code>
        <description>[java] SimplifyConditional can not detect the case if(foo != null &amp;&amp; (foo instanceof Bar)) #2541</description>
        <expected-problems>4</expected-problems>
        <code><![CDATA[
import java.util.List;
public class Foo {
    public void bar(Object temp) {
        if (temp != null && (temp instanceof List<?>)) {}
        if (temp != null && temp instanceof List<?>) {}

        if (temp != null && (temp instanceof List)) {}
        if (temp != null && temp instanceof List) {}
    }
}
]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check separated from instanceof by a &amp;&amp; chain</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object x, Object y) {
        if (x != null && y != null && x instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check separated from !(instanceof) by a || chain</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object x, Object y) {
        if (x == null || y == null || !(x instanceof String)) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 intermediate operand using the subject: the fix is reordering, not dropping</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>Move the instanceof check and remove the unnecessary null check to simplify this conditional</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(java.net.Socket socket) {
        if (socket != null && socket.isConnected() && socket instanceof javax.net.ssl.SSLSocket) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 subject used only as an argument of an intermediate operand still counts as a use</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>Move the instanceof check and remove the unnecessary null check to simplify this conditional</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s != null && other.equals(s) && s instanceof String) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 intermediate operand using the subject in a || chain: the fix is reordering, not dropping</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>Move the instanceof check and remove the unnecessary null check to simplify this conditional</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s == null || s.equals(other) || !(s instanceof String)) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check after the instanceof in a &amp;&amp; chain is redundant</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s instanceof String && other != null && s != null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 operand using the subject after the instanceof is guarded by it, the null check at the end can be dropped</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s instanceof String && s.equals(other) && s != null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 operand using the subject after the instanceof is guarded by it, the null check before the instanceof can be dropped</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s != null && s instanceof String && s.equals(other)) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check after the !(instanceof) in a || chain is redundant</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (other == null || !(s instanceof String) || s == null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 instanceof in the middle of the chain, null check after it</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (other != null && s instanceof String && s != null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 two instanceofs on the same subject share one redundant null check</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object s) {
        if (s != null && s instanceof String && s instanceof CharSequence) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 two negated instanceofs on the same subject share one redundant null check</description>
        <expected-problems>1</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object x, Object s) {
        if (x == null || !(s instanceof String) || !(s instanceof CharSequence) || s == null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 several redundant null checks on the same subject in one chain are each reported</description>
        <expected-problems>2</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s) {
        if (s != null && s instanceof String && s != null) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 a negated instanceof on the subject in between cannot throw, the null check can be dropped</description>
        <expected-problems>1</expected-problems>
        <expected-messages>
            <message>No need to check for null before an instanceof</message>
        </expected-messages>
        <code><![CDATA[
public class Foo {
    void bar(Object s) {
        if (s != null && !(s instanceof String) && s instanceof CharSequence) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check in a parenthesized || operand inside a &amp;&amp; chain is not reported</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s instanceof String && (other == null || s != null)) {}
    }
}
        ]]></code>
    </test-code>

    <test-code>
        <description>#6513 null check outside a parenthesized &amp;&amp; operand of a || chain is not reported</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
public class Foo {
    void bar(Object s, Object other) {
        if (s == null || (other != null && !(s instanceof String))) {}
    }
}
        ]]></code>
    </test-code>
</test-data>
