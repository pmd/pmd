<?xml version="1.0" encoding="UTF-8"?>
<test-data
    xmlns="http://pmd.sourceforge.net/rule-tests"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">
    <test-code>
        <description>Simple case</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
BEGIN
FOR c IN (SELECT * FROM companies) LOOP
  NULL;
END LOOP;
END;
/
        ]]></code>
        <source-type>plsql</source-type>
    </test-code>

    <test-code>
        <description>Nested loops - good example</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
BEGIN
FOR c_cmp IN (SELECT * FROM companies) LOOP
  FOR c_con IN (SELECT * FROM contacts) LOOP
    FOR c_pa IN (SELECT * FROM parties) LOOP
      NULL;
    END LOOP;
  END LOOP;
END LOOP;
END;
/
        ]]></code>
        <source-type>plsql</source-type>
    </test-code>

    <test-code>
        <description>Nested loops - bad example</description>
        <expected-problems>3</expected-problems>
        <expected-linenumbers>2,3,4</expected-linenumbers>
        <code><![CDATA[
BEGIN
FOR c1 IN (SELECT * FROM companies) LOOP
  FOR c2 IN (SELECT * FROM contacts) LOOP
    FOR c3 IN (SELECT * FROM parties) LOOP
      NULL;
    END LOOP;
  END LOOP;
END LOOP;
END;
/
        ]]></code>
        <source-type>plsql</source-type>
    </test-code>
</test-data>