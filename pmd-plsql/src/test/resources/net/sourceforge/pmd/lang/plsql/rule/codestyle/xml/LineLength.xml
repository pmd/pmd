<?xml version="1.0" encoding="UTF-8"?>
<test-data
    xmlns="http://pmd.sourceforge.net/rule-tests"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/rule-tests http://pmd.sourceforge.net/rule-tests_1_0_0.xsd">

    <test-code>
        <description>All lines are fine - default line length</description>
        <expected-problems>0</expected-problems>
        <code><![CDATA[
BEGIN
    SELECT cmer_id
        ,version
        ,cmp_id
    BULK COLLECT INTO v_cmer_ids
        ,v_versions
        ,v_cmp_ids
    FROM cmer;
END;
/
        ]]></code>
    </test-code>

    <test-code>
        <description>One line is too long</description>
        <rule-property name="maxLineLength">30</rule-property>
        <expected-problems>1</expected-problems>
        <expected-linenumbers>5</expected-linenumbers>
        <expected-messages>
            <message>The line is too long (32 characters). Only 30 characters are allowed.</message>
        </expected-messages>
        <code><![CDATA[
BEGIN
    SELECT cmer_id
        ,version
        ,cmp_id
    BULK COLLECT INTO v_cmer_ids
        ,v_versions
        ,v_cmp_ids
    FROM cmer;
END;
/
        ]]></code>
    </test-code>

    <test-code>
        <description>Multiple lines are too long - only first is reported</description>
        <rule-property name="maxLineLength">30</rule-property>
        <expected-problems>1</expected-problems>
        <expected-linenumbers>2</expected-linenumbers>
        <expected-messages>
            <message>The line is too long (46 characters). Only 30 characters are allowed.</message>
        </expected-messages>
        <code><![CDATA[
BEGIN
                                SELECT cmer_id
                                    ,version
                                    ,cmp_id
                                BULK COLLECT INTO v_cmer_ids
                                    ,v_versions
                                    ,v_cmp_ids
                                FROM cmer;
END;
/
        ]]></code>
    </test-code>

    <test-code>
        <description>Multiple lines are too long - report all</description>
        <rule-property name="maxLineLength">30</rule-property>
        <rule-property name="eachLine">true</rule-property>
        <expected-problems>7</expected-problems>
        <expected-linenumbers>2,3,4,5,6,7,8</expected-linenumbers>
        <code><![CDATA[
BEGIN
                                SELECT cmer_id
                                    ,version
                                    ,cmp_id
                                BULK COLLECT INTO v_cmer_ids
                                    ,v_versions
                                    ,v_cmp_ids
                                FROM cmer;
END;
/
        ]]></code>
    </test-code>
    <test-code>
    <description>Report on correct line</description>
    <rule-property name="maxLineLength">80</rule-property>
    <expected-problems>1</expected-problems>
    <expected-linenumbers>23</expected-linenumbers>
    <code><![CDATA[
/* This is a multi line comment
 * The next comment line has no text

 * The next comment line starts with a space.
 *  x, y, z...
 */

-- This is a single line comment containing single (') and double quotes (").
INSERT INTO users(first_name, last_name) VALUES('Juergen', 'Hoeller');
-- This is also a comment.
/*-------------------------------------------
-- A fancy multi-line comment that puts
-- single line comments inside of a multi-line
-- comment block.
Moreover, the block comment end delimiter
appears on a line that can potentially also
be a single-line comment if we weren't
already inside a multi-line comment run.

And here's a line containing single and double quotes (").
-------------------------------------------*/
 INSERT INTO
users(first_name, last_name)    -- This is a single line comment containing the block-end-comment sequence here */ but it's still a single-line comment
VALUES( 'Sam'     -- first_name
      , 'Brannen' -- last_name
);--
        ]]></code>
</test-code>
</test-data>
