<?xml version="1.0"?>

<ruleset name="Strings Rules">
  <description>
These rules deal with different problems that can occur with String manipulation.
  </description>


   <rule name="StringInstantiation"
        message="Avoid instantiating String objects; this is usually unnecessary."
        class="net.sourceforge.pmd.rules.StringInstantiationRule">
    <description>
    Avoid instantiating String objects; this is usually unnecessary.
    </description>

    <example>
<![CDATA[
public class Foo {
 private String bar = new String("bar"); // just do a String bar = "bar";
}
]]>
    </example>
    </rule>

    <rule name="AvoidDuplicateLiterals"
        message="A String appears {0} times in this file; the first occurrence is on line {1}"
        class="net.sourceforge.pmd.rules.AvoidDuplicateLiteralsRule">
    <description>
Avoid Duplicate Literals detects when a duplicate literal Strings are embedded within your code.  Code like this can usually be
improved by declaring the String as a constant field.
    </description>
    <example>
<![CDATA[
public class Foo {
 private void bar() {
    buz("Howdy");
 }
 private void buz(String x) {
    String x = "Howdy";
 }
}
]]>
    </example>
  </rule>

   <rule name="StringToString"
        message="Avoid calling toString() on String objects; this is unnecessary"
        class="net.sourceforge.pmd.rules.StringToStringRule">
    <description>
    Avoid calling toString() on String objects; this is unnecessary
    </description>

    <example>
<![CDATA[
public class Foo {
 private String baz() {
  String bar = "howdy";
  return bar.toString();
 }
}
]]>
    </example>
    </rule>

</ruleset>



